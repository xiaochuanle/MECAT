#ifndef READS_CORRECTION_H
#define READS_CORRECTION_H

#include <vector>
#include <cstring>

#include "reads_correction_aux.h"

int reads_correction_m4(ReadsCorrectionOptions& rco);

#endif // READS_CORRECTION_H
